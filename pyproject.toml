[project]
name = "kx"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "typer",
]

[project.scripts]
kx = "kx.main:app"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
